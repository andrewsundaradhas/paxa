# React Native 0.74 pins an exact peer (react@18.2.0) while this project tracks
# react@18.3.1, so strict peer resolution fails. legacy-peer-deps keeps installs
# working across the monorepo (standard for React Native projects).
legacy-peer-deps=true
